export type Role = {
  company: string;
  title: string;
  period: string;
  location: string;
  summary: string[];
  tech: string[];
};

export type SkillGroup = {
  label: string;
  skills: string[];
};
